name: Gallery Builder

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Test
        run: |
          echo "=================================="
          echo "GitHub Actions działa poprawnie!"
          echo "=================================="
          ls -la
